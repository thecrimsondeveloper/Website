# LOCAL

1. Prefer the existing local checkout.
2. Record the current branch and commit SHA.
3. Verify the working tree state so evidence reflects the code actually rendered.
4. Use the system browser/GPU when available.
5. Fall back to software rendering only when required.
6. Write evidence to the target repository unless the caller provides another output path.
